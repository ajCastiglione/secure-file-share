<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Secure File Share</title>
        <link rel="stylesheet" type="text/css" href="/output.css" />
    </head>
    <body>
        <% if(locals.error) { %>
        <div class="error">Incorrect Password</div>
        <% } %>
        <form class="form" method="POST">
            <label
                for="password"
                class="text-xs place-self-center font-semibold inline-block py-1 px-2 rounded text-teal-400 uppercase last:mr-0 mr-1"
                >Password:</label
            >
            <input
                type="password"
                name="password"
                id="password"
                required
                class="shadow appearance-none border border-teal-500 rounded w-full py-1 px-3 mb-1 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
            />
            <button
                type="submit"
                class="rounded-full font-semibold bg-teal-500 text-white py-1 hover:bg-teal-700 ease-linear duration-100"
            >
                Download
            </button>
            <a
                href="<%= locals.home %>"
                class="rounded-full block w-full text-center font-semibold bg-red-500 text-white py-1 hover:bg-red-700 ease-linear duration-100"
            >
                Return Home
            </a>
        </form>
    </body>
</html>
